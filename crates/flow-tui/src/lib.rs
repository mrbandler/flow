//! Terminal User Interface for Flow.

use miette::Result;

/// Runs the Flow TUI application.
///
/// # Errors
///
/// Returns an error if the TUI fails to initialize or encounters
/// an unrecoverable runtime error during execution.
pub async fn run() -> Result<()> {
    todo!("Running TUI");
}
