//! Flow GUI - Desktop graphical user interface for Flow.

use miette::Result;

/// Runs the Flow desktop GUI application.
///
/// # Errors
///
/// Returns an error if the GUI fails to initialize or encounters an unrecoverable runtime error.
pub async fn run() -> Result<()> {
    todo!("Running GUI");
}
