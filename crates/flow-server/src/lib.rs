//! Flow server library.

use miette::Result;

/// Runs the Flow server.
///
/// # Errors
///
/// Returns an error if the server fails to start or encounters a unrecoverable runtime error.
pub async fn run() -> Result<()> {
    todo!("Running server");
}
