# cargo-deny configuration
# https://embarkstudios.github.io/cargo-deny/

[graph]
targets = [
    "x86_64-unknown-linux-gnu",
    "x86_64-unknown-linux-musl",
    "aarch64-unknown-linux-gnu",
    "x86_64-apple-darwin",
    "aarch64-apple-darwin",
    "x86_64-pc-windows-msvc",
]
exclude-dev = true

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
unmaintained = "workspace"
yanked = "warn"
unsound = "workspace"
ignore = []

[licenses]
confidence-threshold = 0.8
unused-allowed-license = "allow"
unused-license-exception = "allow"
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "0BSD",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "CC0-1.0",
    "MPL-2.0",
    "BSL-1.0",
]
# Workspace crates use AGPL-3.0-or-later. Others can use and modify the code,
# but if they run a modified version as a network service (e.g., SaaS), they
# must make their source code available to users (see AGPL Section 13).
exceptions = [
    { name = "flow", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-errors", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-common", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-app", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-cli", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-core", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-gui", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-server", allow = ["AGPL-3.0-or-later"] },
    { name = "flow-tui", allow = ["AGPL-3.0-or-later"] },
]

[[licenses.clarify]]
name = "ring"
expression = "MIT AND ISC AND OpenSSL"
license-files = [{ path = "LICENSE", hash = 0xbd0eed23 }]

[bans]
multiple-versions = "allow"
deny = []
allow = []
skip = []
skip-tree = []
wildcards = "allow"

[sources]
unknown-registry = "deny"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []

[sources.allow-org]
# github = ["my-org"]
